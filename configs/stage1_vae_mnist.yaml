seed: 0
run_dir: runs/stage1
data:
  root: data/mnist
  batch_size: 128
  num_workers: 0
model:
  hidden_dim: 400
  latent_dim: 20
optim:
  lr: 1.0e-3
train:
  max_steps: 3000
  beta: 1.0
  log_every: 100
  val_every: 500
  sample_every: 500
  grad_clip: 1.0
sample:
  num: 64
