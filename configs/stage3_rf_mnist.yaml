seed: 0
run_dir: runs/stage3
data:
  root: data/mnist
  batch_size: 128
  num_workers: 0
model:
  base_channels: 48
  time_dim: 128
optim:
  lr: 2.0e-4
train:
  max_steps: 4000
  log_every: 100
  val_every: 1000
  sample_every: 1000
  grad_clip: 1.0
sample:
  num: 64
  nfe_list: [8, 16, 50]
