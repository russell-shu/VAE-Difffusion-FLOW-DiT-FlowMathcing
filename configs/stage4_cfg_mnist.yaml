seed: 0
run_dir: runs/stage4
data:
  root: data/mnist
  batch_size: 128
  num_workers: 0
model:
  base_channels: 48
  time_dim: 128
  num_classes: 10
  class_drop_prob: 0.1
optim:
  lr: 2.0e-4
train:
  max_steps: 4000
  log_every: 100
  val_every: 1000
  sample_every: 1000
  grad_clip: 1.0
sample:
  num_per_class: 8
  nfe: 16
  alphas: [0.0, 1.0, 4.0, 10.0]
